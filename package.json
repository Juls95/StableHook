{
  "name": "stableyield-hook",
  "version": "0.1.0",
  "description": "Uniswap V4 hook for optimizing stablecoin yield through EigenLayer AVS",
  "scripts": {
    "compile": "forge build",
    "test": "forge test",
    "lint": "solhint 'contracts/**/*.sol'"
  },
  "devDependencies": {
    "solhint": "^4.0.0"
  }
}

